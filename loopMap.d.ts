/**
 * @overview loop-map
 * @author Absolutely Amir <mr.amirka@ya.ru>
 */

import { fn } from "./global";

declare const loopMap: (length: number, fn: fn, output?: any, start?: number) => any[];
export = loopMap;
